node_modules
dist
build
*.log

# generated — produced/validated by their own generators, not prettier
# (formatting these here conflicts with gen scripts / the form-schema CI gate)
.docusaurus/
**/docs/llms/
# llms-index.json пишет scripts/generate-llms-txt через JSON.stringify; prettier
# переформатирует его иначе, и тогда КАЖДАЯ сборка помечает файл изменённым
**/llms-index.json
# baseline eval-гейта пишет packages/reformer-mcp/eval/runner.mjs через JSON.stringify;
# prettier сжимает toolset.tools в одну строку — тогда КАЖДЫЙ --save оставлял бы файл
# изменённым после lint-staged
docs/mcp-eval/baseline.json
projects/react-playground/src/pages/demo/complex-multy-step-form-renderer-json/form-schema.schema.json

# planning artifacts — authored ad-hoc by plan mode / agents, not prod docs
docs/plans/

# Handlebars-шаблоны промптов MCP: prettier ломает {{code}} внутри ```typescript
# fence в невалидный `{ { code; } }` (парсит как объект-литерал) — подстановка молча
# перестаёт работать. Это .md по расширению, но не markdown, а Handlebars.
packages/reformer-mcp/src/prompts/templates/

# beads issue tracker — файлы ведёт bd (jsonl/yaml/backup), не prettier
.beads/
