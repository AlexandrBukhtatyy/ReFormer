// base — pure shadcn Sheet (Radix Dialog как боковая панель: Sheet / SheetTrigger /
// SheetClose / SheetPortal / SheetOverlay / SheetContent (side: top|right|bottom|left) /
// SheetHeader / SheetFooter / SheetTitle / SheetDescription). Не form-control —
// реэкспорт всех под-компонентов.
export {
  Sheet,
  SheetClose,
  SheetContent,
  SheetDescription,
  SheetFooter,
  SheetHeader,
  SheetOverlay,
  SheetPortal,
  SheetTitle,
  SheetTrigger,
} from './variants/base/sheet-base';
