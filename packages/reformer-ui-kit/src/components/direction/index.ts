// base — pure shadcn Direction: тонкая обёртка над Radix Direction.DirectionProvider
// (RTL/LTR-контекст для Radix-примитивов) + хук useDirection.
export { DirectionProvider, useDirection } from './variants/base/direction-base';
