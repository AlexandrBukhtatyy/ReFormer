// base — pure shadcn Popover (compound поверх Radix Popover:
// Popover / PopoverTrigger / PopoverContent / PopoverAnchor + Header / Title / Description).
export {
  Popover,
  PopoverTrigger,
  PopoverContent,
  PopoverAnchor,
  PopoverHeader,
  PopoverTitle,
  PopoverDescription,
} from './variants/base/popover-base';
