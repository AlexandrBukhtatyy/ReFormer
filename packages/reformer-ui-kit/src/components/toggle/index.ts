// base — shadcn Toggle (Radix, button + aria-pressed) + проп tooltip. Компонент для формы
// (value: boolean; НЕ inline-label — подпись сверху от FormField).
export { Toggle, toggleVariants, type ToggleFormProps } from './variants/base/toggle-base';

// props-схема.
export { toggleBasePropsSchema } from './variants/base/toggle-base.props';
