// base — pure shadcn Dialog (Radix Dialog compound: Dialog / DialogTrigger / DialogPortal /
// DialogOverlay / DialogClose / DialogContent / DialogHeader / DialogFooter / DialogTitle /
// DialogDescription). Не form-control — реэкспорт всех под-компонентов.
export {
  Dialog,
  DialogClose,
  DialogContent,
  DialogDescription,
  DialogFooter,
  DialogHeader,
  DialogOverlay,
  DialogPortal,
  DialogTitle,
  DialogTrigger,
} from './variants/base/dialog-base';
