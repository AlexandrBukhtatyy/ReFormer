name: Release @reformer/renderer-react

on:
  push:
    branches: [main, develop]
    paths:
      - 'packages/reformer-renderer-react/**'
      - '.github/workflows/release-renderer-react.yml'

concurrency:
  # Все release-* workflow share одну очередь, чтобы предотвратить race
  # на git push refs/notes/semantic-release. Параллельные releases ломали
  # друг друга с "! [rejected] (fetch first)". cancel-in-progress: false
  # — новый run ждёт текущий, не отменяет.
  group: semantic-release-notes
  cancel-in-progress: false

jobs:
  release:
    name: Release @reformer/renderer-react
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: write
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GH_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: npm ci

      - name: Build @reformer/core (dependency)
        run: npm run build -w @reformer/core

      - name: Build @reformer/renderer-react
        run: npm run build -w @reformer/renderer-react

      - name: Fetch git notes
        run: git fetch origin refs/notes/semantic-release:refs/notes/semantic-release || true

      - name: Release
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npx semantic-release
        working-directory: packages/reformer-renderer-react
