name: CI

on:
  push:
    branches: [develop]
  pull_request:
    # main — для promote develop → main; develop — чтобы feature → develop
    # PR проверялись ДО merge (раньше PR в develop шли без CI).
    branches: [main, develop]

jobs:
  test:
    uses: ./.github/workflows/test.yml
