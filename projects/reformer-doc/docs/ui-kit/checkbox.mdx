---
id: checkbox
title: Checkbox
sidebar_label: Checkbox
---

import { ComponentDoc } from '@site/src/components/demo';
import { checkboxDocConfig } from '@site/src/components/demo/examples/checkbox';

# Checkbox

Булев чекбокс с подписью справа. Контракт `value` / `onChange` — `boolean`.
`FormField` детектит `Checkbox` и не рендерит верхний label.

<ComponentDoc config={checkboxDocConfig} />
