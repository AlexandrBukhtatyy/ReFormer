---
id: select
title: Select
sidebar_label: Select
---

import { ComponentDoc } from '@site/src/components/demo';
import { selectDocConfig } from '@site/src/components/demo/examples/select';

# Select

Выпадающий список на Radix. Вариант **base** — чистый compound для ручной сборки
(`Select` / `SelectTrigger` / `SelectContent` / `SelectItem`). Вариант **async** — высокоуровневый:
inline `options` или асинхронный источник `resource` (стратегии `static` / `preload` / `partial`)
с поиском, пагинацией и очисткой. Для форм — `SelectAsync` (registry `Select`), кладётся в `component` как есть.

<ComponentDoc config={selectDocConfig} />
