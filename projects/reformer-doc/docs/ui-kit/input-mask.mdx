---
id: input-mask
title: InputMask
sidebar_label: InputMask
---

import { ComponentDoc } from '@site/src/components/demo';
import { inputMaskDocConfig } from '@site/src/components/demo/examples/input-mask';

# InputMask

Текстовое поле с маской-подсказкой поверх pure shadcn `Input`. Шаблон `mask` (символ `9` — цифра,
прочие символы — литералы: `+`, `-`, `(`, `)`, пробел, точка) показывается в `placeholder` и помечает
формат; автоматической вставки литералов **нет**. Значение — `string | null` (пустой ввод → `null`).
Контракт уже value-based — в форме `InputMask` кладётся в `component` как есть.

<ComponentDoc config={inputMaskDocConfig} />
