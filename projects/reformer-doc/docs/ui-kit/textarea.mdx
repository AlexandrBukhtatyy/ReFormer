---
id: textarea
title: Textarea
sidebar_label: Textarea
---

import { ComponentDoc } from '@site/src/components/demo';
import { textareaDocConfig } from '@site/src/components/demo/examples/textarea';

# Textarea

Многострочное текстовое поле на pure shadcn `Textarea`. Значение — `string | null`
(пустой ввод → `null`). Для форм — `TextareaField`, биндится к M1-полю через `nativeInputAdapter`.

<ComponentDoc config={textareaDocConfig} />
