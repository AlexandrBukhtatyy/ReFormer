/**
 * Идентификаторы визуального редактора схемы: сам плагин и его модельный документ.
 *
 * Отдельный лист графа, а не строка в plugin.ts: идентификатор нужен КОМПОЗИЦИИ (портам
 * в shell/boot/ports), а импорт значения из бареля втягивает весь плагин в стартовый граф.
 * Плагин приезжает своим файлом ровно пока сюда не просочилось ничего тяжёлого — этот модуль
 * обязан остаться без импортов значений.
 *
 * @module plugins/editor-schema/contract
 */

/** Идентификатор плагина: пространство имён во всех реестрах и в словаре. */
export const SCHEMA_EDITOR_PLUGIN_ID = 'editor-schema';

/**
 * Идентификатор провайдера.
 *
 * Он же — «вид активного ресурса» в контексте применимости (`WhenContext.activeResourceKind`
 * равен `providerId` модельного документа), поэтому имя выбрано предметным, а не служебным:
 * предикат команды читается как «когда открыта схема формы».
 */
export const SCHEMA_MODEL_PROVIDER_ID = 'form.schema';
