{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://reformer.dev/schemas/credit-application.schema.json",
  "title": "Credit application form schema (M1, generated)",
  "description": "GENERATED by react-playground/scripts/gen-form-json-schema.ts from createCreditApplicationRegistry — DO NOT EDIT BY HAND. Materialized snapshot of the base form-DSL meta-schema (@reformer/renderer-json) with componentOp tightened to the registry enum. Re-run `npm run gen:form-schema` after changing the registry.",
  "type": "object",
  "required": [
    "root"
  ],
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string"
    },
    "version": {
      "type": "string"
    },
    "root": {
      "$ref": "#/definitions/node"
    }
  },
  "definitions": {
    "modelOp": {
      "type": "string",
      "pattern": "^\\$model\\(.+\\)$",
      "description": "Model binding: \"$model(path)\" — path resolved at runtime (not validated here)."
    },
    "componentOp": {
      "type": "string",
      "description": "Registry component: \"$component(Name)\". Name is tightened to a registry enum by buildFormSchemaMetaSchema.",
      "enum": [
        "$component(Input)",
        "$component(InputPassword)",
        "$component(InputMask)",
        "$component(Textarea)",
        "$component(Select)",
        "$component(Checkbox)",
        "$component(RadioGroup)",
        "$component(FileUpload)",
        "$component(Box)",
        "$component(Section)",
        "$component(Collapsible)",
        "$component(AsyncBoundary)",
        "$component(FormField)",
        "$component(Button)",
        "$component(RendererFormWizard)",
        "$component(Step)",
        "$component(ResidenceAddressSection)",
        "$component(UnemployedWarning)",
        "$component(ConfirmationInfoBlock)",
        "$component(HighPaymentWarning)",
        "$component(LoanSummarySection)",
        "$component(ApplicantSummarySection)",
        "$component(SubmitWarning)",
        "$component(NextStepsInfo)",
        "$component(ElectronicSignatureHint)",
        "$component($fieldWrapper)"
      ]
    },
    "htmlOp": {
      "type": "string",
      "description": "Native HTML tag: \"$html(div)\". The tag is checked against the presentational whitelist (isAllowedHtmlTag) by validateFormSchema and by the converter; buildFormSchemaMetaSchema tightens it to an enum for IDE completion.",
      "enum": [
        "$html(div)",
        "$html(section)",
        "$html(article)",
        "$html(aside)",
        "$html(header)",
        "$html(footer)",
        "$html(nav)",
        "$html(main)",
        "$html(figure)",
        "$html(figcaption)",
        "$html(details)",
        "$html(summary)",
        "$html(fieldset)",
        "$html(legend)",
        "$html(p)",
        "$html(span)",
        "$html(h1)",
        "$html(h2)",
        "$html(h3)",
        "$html(h4)",
        "$html(h5)",
        "$html(h6)",
        "$html(strong)",
        "$html(b)",
        "$html(em)",
        "$html(i)",
        "$html(u)",
        "$html(s)",
        "$html(small)",
        "$html(mark)",
        "$html(sub)",
        "$html(sup)",
        "$html(code)",
        "$html(pre)",
        "$html(kbd)",
        "$html(samp)",
        "$html(var)",
        "$html(blockquote)",
        "$html(cite)",
        "$html(q)",
        "$html(abbr)",
        "$html(time)",
        "$html(address)",
        "$html(label)",
        "$html(br)",
        "$html(hr)",
        "$html(a)",
        "$html(ul)",
        "$html(ol)",
        "$html(li)",
        "$html(dl)",
        "$html(dt)",
        "$html(dd)",
        "$html(table)",
        "$html(caption)",
        "$html(colgroup)",
        "$html(col)",
        "$html(thead)",
        "$html(tbody)",
        "$html(tfoot)",
        "$html(tr)",
        "$html(th)",
        "$html(td)",
        "$html(img)",
        "$html(picture)",
        "$html(source)",
        "$html(video)",
        "$html(audio)",
        "$html(track)",
        "$html(progress)",
        "$html(meter)"
      ]
    },
    "textContent": {
      "description": "Text content of a node: a literal, an operator string (\"$model(path)\" for reactive model values, \"$locale(key)\" for localized strings), or an array of parts joined without a separator.",
      "$comment": "Types are listed as separate anyOf branches rather than a union \"type\": [\"string\",\"number\"] — ajv's strict mode rejects union type keywords unless allowUnionTypes is on, and validateFormSchema compiles this schema without it.",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "number"
        },
        {
          "type": "array",
          "items": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "number"
              }
            ]
          }
        }
      ]
    },
    "operatorOp": {
      "type": "string",
      "pattern": "^\\$(model|component|html|dataSource|fn|locale)\\(.+\\)$",
      "description": "Any DSL operator string (\"$model/$component/$dataSource/$fn/$locale(...)\") used as an escape hatch on a componentProps value (e.g. \"options\": \"$dataSource(LOAN_TYPES)\"). The component-props validator wraps every prop in anyOf:[honest type, operatorOp] via allowOperatorStrings, so an operator string is accepted wherever a literal value is."
    },
    "node": {
      "$comment": "Discriminated by which operator key is present (array→value→component), mirroring the isArrayNode/isFieldNode/isContainerNode runtime guards. Using oneOf here made ajv (allErrors) report failures against all three branches for any single mistake, burying the real cause; if/then/else surfaces only the relevant branch's errors.",
      "if": {
        "type": "object",
        "required": [
          "array"
        ]
      },
      "then": {
        "$ref": "#/definitions/arrayNode"
      },
      "else": {
        "if": {
          "type": "object",
          "required": [
            "value"
          ]
        },
        "then": {
          "$ref": "#/definitions/fieldNode"
        },
        "else": {
          "$ref": "#/definitions/containerNode"
        }
      }
    },
    "fieldNode": {
      "type": "object",
      "required": [
        "value"
      ],
      "additionalProperties": false,
      "properties": {
        "selector": {
          "type": "string"
        },
        "value": {
          "$ref": "#/definitions/modelOp"
        },
        "component": {
          "$ref": "#/definitions/componentOp"
        },
        "componentProps": {
          "type": "object"
        },
        "wrapper": {
          "$ref": "#/definitions/node"
        }
      }
    },
    "arrayNode": {
      "type": "object",
      "required": [
        "array",
        "item"
      ],
      "additionalProperties": false,
      "properties": {
        "selector": {
          "type": "string"
        },
        "array": {
          "$ref": "#/definitions/modelOp"
        },
        "item": {
          "type": "object",
          "required": [
            "$template"
          ],
          "additionalProperties": false,
          "properties": {
            "$template": {
              "$ref": "#/definitions/node"
            }
          }
        },
        "initialValue": {
          "type": "object"
        },
        "componentProps": {
          "type": "object"
        }
      }
    },
    "containerNode": {
      "type": "object",
      "required": [
        "component"
      ],
      "additionalProperties": false,
      "properties": {
        "selector": {
          "type": "string"
        },
        "component": {
          "anyOf": [
            {
              "$ref": "#/definitions/componentOp"
            },
            {
              "$ref": "#/definitions/htmlOp"
            }
          ]
        },
        "componentProps": {
          "type": "object"
        },
        "text": {
          "$ref": "#/definitions/textContent"
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/node"
          }
        }
      }
    }
  }
}
