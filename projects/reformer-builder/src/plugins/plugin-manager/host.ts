/**
 * Что осталось порту после переезда каталога в привилегированную службу: перевод.
 *
 * ## Куда уехало остальное
 *
 * Список и глаголы над ним (`list`, `enable`, `disable`, `reload`, `setDev`, `refresh`,
 * установка и откат) — в `@reformer/builder-plugin-api`, возможность
 * `reformer.plugins.catalog` с правом `plugins.manage`. Портом они существовали ровно потому,
 * что собрать его внешнему плагину некому: распоряжаться плагинами мог только встроенный,
 * и никакого решения человек при этом не принимал. Со службой и правом решение появилось.
 *
 * Перевод порту нужен и дальше: он берётся из `ctx.i18n` и правом не охраняется — словарь
 * плагина не привилегия.
 *
 * @module plugins/plugin-manager/host
 */

/**
 * Перевод в пространстве имён плагина. Функция, а не хук: пункты палитры собираются
 * в момент запроса, а не в отрисовке (см. одноимённый тип у плагина китов).
 */
export type Translate = (key: string, params?: Record<string, unknown>) => string;
