/**
 * Словарь плагина предпросмотра — тем же способом, что у шаблонов и кодогена: JSON рядом.
 *
 * Заголовки КОМАНД лежат здесь, а не в словаре Host, и это не расхождение с плагином файлов:
 * ключ команды ищется в словаре её ВЛАДЕЛЬЦА, а пространство имён — отдельное измерение,
 * а не приставка к ключу. Плагин, у которого свой словарь есть, обязан держать заголовки
 * в нём — это проверяет тест композиции «ни одна команда не показывает маркер промаха».
 *
 * @module plugins/editor-markdown/messages
 */

import en from './locales/en.json';
import ru from './locales/ru.json';

/** Локаль → ключ (без пространства имён) → сообщение. */
export const MARKDOWN_MESSAGES: Readonly<Record<string, Readonly<Record<string, string>>>> =
  Object.freeze({ ru, en });
