/**
 * Идентификатор плагина поверхностей формы ReFormer.
 *
 * Отдельный лист графа, а не строка в `plugin.ts`: идентификатор нужен КОМПОЗИЦИИ (порту
 * превью в `shell/boot/ports`), а импорт значения из бареля втянул бы весь плагин в стартовый
 * граф. Модуль обязан остаться без импортов значений — кроме собственного манифеста: JSON это
 * лист, и кода плагина за ним нет.
 *
 * @module plugins/preview-runtime/contract
 */

import manifest from './manifest.json';

/** Идентификатор плагина: пространство имён во всех реестрах и в словаре. */
export const PREVIEW_RUNTIME_PLUGIN_ID = manifest.id;
