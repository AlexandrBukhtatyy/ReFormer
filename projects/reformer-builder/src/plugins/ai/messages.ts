/**
 * Словарь плагина ассистента.
 *
 * **Почему словарь везёт плагин, а не Host.** Строки ассистента — предметные («Локальный
 * (OpenAI-совместимый)», «Не задан ключ API»), и Host про них знать не должен. Разрешаются они в
 * пространстве имён внёсшего плагина, поэтому ключи здесь БЕЗ префикса `ai.`: пространство
 * добавляет тот, кто регистрирует словарь. Тот же приём, что у `plugins/files/messages`.
 *
 * Регистрирует словарь композиция: `PluginContext` сервиса локализации не содержит, и это не
 * упущение — вклад в словарь не снимается вместе с плагином, а значит и не может быть частью его
 * подписок.
 *
 * **Чего здесь нет.** Всего, что уходит МОДЕЛИ: системного промпта, описаний инструментов, текстов
 * ошибок гейта. Они по-английски и переводу не подлежат — модель читает их сама, и перевод сделал
 * бы поведение зависящим от языка интерфейса. Это не недосмотр локализации, а её граница.
 *
 * @module plugins/ai/messages
 */

import en from './locales/en.json';
import ru from './locales/ru.json';

/** Локаль → ключ (без пространства имён) → сообщение. */
export const AI_MESSAGES: Readonly<Record<string, Readonly<Record<string, string>>>> =
  Object.freeze({ ru, en });
