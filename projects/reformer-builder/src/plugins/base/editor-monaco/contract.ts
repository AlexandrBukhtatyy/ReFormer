/**
 * Идентификаторы редактора Monaco: сам плагин и его редактор.
 *
 * Отдельный лист графа, а не строка в `plugin.ts`, — по той же причине, что и у визуального
 * редактора схемы (`plugins/reformer/editor/contract`): идентификатор нужен КОМПОЗИЦИИ
 * (`shell/boot/ports/monaco` ставит его приставкой к словарю плагина), а импорт значения
 * из бареля втягивает в стартовый граф весь плагин — вместе с Monaco.
 *
 * Это и был единственный довод, по которому плагин объявлял `builtin.loading: "eager"`:
 * не «его нельзя отложить», а «его всё равно уже втянули». Довод снят выносом; модуль обязан
 * остаться без импортов значений — кроме собственного манифеста: JSON это лист, и кода
 * плагина за ним нет.
 *
 * @module plugins/base/editor-monaco/contract
 */

import manifest from './manifest.json';

/** Идентификатор плагина: пространство имён во всех реестрах и в словаре. */
export const MONACO_PLUGIN_ID = manifest.id;

/** Идентификатор вклада редактора: ключ состояния вида и адрес в диагностике. */
export const MONACO_EDITOR_ID = 'editor.monaco';
