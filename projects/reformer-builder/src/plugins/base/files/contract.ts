/**
 * Идентификатор плагина файлов — отдельным листом графа.
 *
 * Тот же приём и та же причина, что у `plugins/reformer/editor/contract` и
 * `plugins/base/editor-monaco/contract`: идентификатор нужен КОМПОЗИЦИИ (порт файлов разрешает
 * им своё пространство имён в словаре, а `boot` регистрирует словарь до первой активации),
 * а импорт значения из бареля втягивает в стартовый граф весь плагин — панель, дерево,
 * операции над файлами.
 *
 * Словарь плагина (`./messages`) — такой же лист и по той же причине; вместе они и есть всё,
 * что композиции нужно от файлов до запуска. Модуль обязан остаться без импортов значений —
 * кроме собственного манифеста: JSON это лист, и кода плагина за ним нет.
 *
 * @module plugins/base/files/contract
 */

import manifest from './manifest.json';

/** Идентификатор плагина: пространство имён во всех реестрах и в словаре. */
export const FILES_PLUGIN_ID = manifest.id;
