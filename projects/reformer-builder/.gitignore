# Logs
logs
*.log
npm-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.sw?

# Корпус знаний ассистента: собирается из llms.txt установленных пакетов
# (npm run generate:knowledge). Версия обязана совпадать со сборкой, а не с моментом,
# когда его последний раз обновили руками, поэтому в git он не едет.
src/plugins/ai/knowledge/generated/
