# Прохождение туториала ReFormer с валидацией

> **Задача**: Пройти полный туториал ReFormer, создав приложение "Credit Application Form" и выявив все проблемы.

## 📋 TL;DR

**Что делать**: Пройти 7 разделов туториала → После каждого раздела проверить UI через Playwright MCP → Документировать проблемы → Создать отчет

**Технологии**: React + Vite + TypeScript + ReFormer + Tailwind CSS + shadcn/ui + Playwright

**Критично**: ⚠️ Не переходить к следующему разделу без успешных тестов Playwright

**Туториал**: 📚 [ReFormer Tutorial - Introduction](http://localhost:3000/ReFormer/docs/tutorial/introduction/overview)

---

## 🚀 Quick Start

```bash
# 1. Создать проект
cd projects
npm create vite@latest tutorial -- --template react-ts

# 2. Установить зависимости
cd tutorial
npm install reformer
npm install -D tailwindcss postcss autoprefixer @playwright/test
npx tailwindcss init -p
npx shadcn-ui@latest init
npx playwright install

# 3. Запустить
npm run dev
```

---

## 📖 Разделы туториала

| #   | Раздел       | Playwright проверки                      | Тесты                    |
| --- | ------------ | ---------------------------------------- | ------------------------ |
| 1   | Introduction | Проект запускается                       | `01-setup.spec.ts`       |
| 2   | Form Schema  | Типы компилируются                       | -                        |
| 3   | Rendering    | Все поля, навигация, shadcn/ui           | `02-rendering/*.spec.ts` |
| 4   | Behaviors    | Вычисляемые поля, условия, синхронизация | `03-behaviors.spec.ts`   |
| 5   | Validation   | Валидаторы, ошибки, async, кросс-поле    | `04-validation.spec.ts`  |
| 6   | Data Flow    | Загрузка, автосохранение, черновики      | `05-data-flow.spec.ts`   |
| 7   | Submission   | Отправка, ошибки, retry, optimistic      | `06-submission.spec.ts`  |

---

## 🔄 Рабочий процесс для каждого раздела

```
1. Прочитать раздел туториала
2. Реализовать код
3. Запустить dev-сервер (npm run dev)
4. Проверить UI через Playwright MCP ⚠️ ОБЯЗАТЕЛЬНО
5. Создать автоматические тесты
6. Запустить тесты (npx playwright test)
7. Документировать проблемы
8. Переходить к следующему разделу ✅
```

---

## 🎭 Playwright MCP Workflow

### Для каждого раздела:

**1. Ручная проверка через MCP**

```typescript
// Через Claude + Playwright MCP:
// 1. Открыть http://localhost:5173
// 2. Использовать mcp__playwright__browser_snapshot
// 3. Взаимодействовать с полями
// 4. Проверить все функции раздела
// 5. При багах - mcp__playwright__browser_take_screenshot
```

**2. Создание автоматических тестов**

```bash
# Попросить Claude создать тесты на основе ручных проверок
# Разместить в tests/e2e/XX-section-name/
```

**3. Запуск тестов**

```bash
npx playwright test tests/e2e/XX-section-name/

# Все зеленые ✅ → Следующий раздел
# Есть красные ❌ → Исправить → Документировать
```

---

## 📝 Документирование проблем

### Категории:

- 🐛 **Баги библиотеки** - Ошибки ReFormer
- 📝 **Ошибки в docs** - Неточности в туториале
- ❓ **Неясности** - Недостаточно объяснений
- 💡 **Улучшения** - Предложения
- ⚠️ **Пропуски** - Отсутствующая информация

### Формат:

```markdown
### [🐛] Краткое описание проблемы

**Раздел**: Rendering
**Файл**: src/components/FormField.tsx:42

**Проблема**: FormField не отображает errors при async validation

**Ожидается**: Показать pending state и затем errors
**Фактически**: Errors не отображаются

**Воспроизведение**:

1. Добавить async validator
2. Заполнить поле
3. Errors не появляются

**Решение**: Добавить useEffect для subscribing на pending state

**Playwright скриншот**: `screenshots/async-validation-bug.png`
```

---

## ⚙️ Настройка проекта

### Tailwind CSS

```javascript
// tailwind.config.js
export default {
  content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
  theme: { extend: {} },
  plugins: [],
};
```

```css
/* src/index.css */
@tailwind base;
@tailwind components;
@tailwind utilities;
```

### shadcn/ui

```bash
# Инициализация
npx shadcn-ui@latest init
# Style: Default | Base color: Slate | CSS variables: Yes

# Установка компонентов по мере необходимости
npx shadcn-ui@latest add button input select checkbox textarea
```

### Playwright

```bash
npm install -D @playwright/test
npx playwright install

# Создать playwright.config.ts с baseURL: http://localhost:5173
```

---

## 📊 Итоговый отчет

**Файл**: `TUTORIAL_REPORT.md`

```markdown
# Отчет о прохождении туториала ReFormer

## Резюме

- Дата: 2025-11-27
- ReFormer: v1.0.0
- Статус: Успешно / С проблемами

## Статистика

- Всего проблем: 15
- 🐛 Баги библиотеки: 3
- 📝 Ошибки в docs: 7
- ❓ Неясности: 3
- 💡 Улучшения: 2
- ⚠️ Пропуски: 0

## Критические проблемы

[Только блокирующие проблемы]

## Детальный отчет по разделам

### Introduction

...

### Rendering

...
[и т.д.]

## Рекомендации

[Общие рекомендации]
```

---

## ✅ Критерии успеха

- [x] Проект создан и запускается
- [x] Tailwind + shadcn/ui настроены
- [x] Все 7 разделов пройдены
- [x] Playwright тесты покрывают все разделы
- [x] Все тесты зеленые
- [x] Проблемы задокументированы
- [x] Отчет создан

---

## 📁 Структура проекта

```
tutorial/
├── src/
│   ├── components/
│   │   ├── ui/              # shadcn/ui
│   │   ├── nested-forms/    # Переиспользуемые формы
│   │   └── steps/           # Шаги формы (6 файлов)
│   ├── types/               # TypeScript типы
│   ├── schemas/             # Схемы форм (7 файлов после декомпозиции)
│   │   ├── behaviors/           # Поведения (8 файлов)
│   │   └── validators/          # Валидаторы (8 файлов)
│   ├── hooks/               # React хуки
│   └── services/            # API, storage
├── tests/
│   └── e2e/                 # Playwright тесты
├── screenshots/             # Скриншоты багов
└── TUTORIAL_VALIDATION_REPORT.md
```

---

## 🎯 Технический стек

| Технология   | Назначение         | Обязательно |
| ------------ | ------------------ | ----------- |
| React        | UI библиотека      | ✅          |
| Vite         | Сборщик            | ✅          |
| TypeScript   | Типизация          | ✅          |
| ReFormer     | Управление формами | ✅          |
| Tailwind CSS | Стилизация         | ✅          |
| shadcn/ui    | UI компоненты      | ✅          |
| Playwright   | E2E тестирование   | ✅          |

---

## ⚠️ Важные правила

1. **Не пропускать Playwright проверки** - после каждого раздела обязательно
2. **Не переходить к следующему разделу** - пока тесты не зеленые
3. **Документировать все баги** - с Playwright скриншотами
4. **Использовать только shadcn/ui** - для UI компонентов
5. **Следовать туториалу точно** - не использовать workarounds без документирования
6. **Фиксировать время** - для каждого раздела

---

## 🔗 Полезные команды

```bash
# Разработка
npm run dev              # Запуск dev-сервера
npm run build            # Сборка проекта
npm run preview          # Preview production build

# Тестирование
npx playwright test                        # Все тесты
npx playwright test tests/e2e/03-*         # Конкретный раздел
npx playwright test --ui                   # UI mode
npx playwright test --debug                # Debug mode
npx playwright show-report                 # Показать отчет

# shadcn/ui
npx shadcn-ui@latest add [component]       # Добавить компонент
npx shadcn-ui@latest diff                  # Обновления

# TypeScript
npx tsc --noEmit                           # Проверка типов
```

---

**Начать**: Создайте проект командой из раздела Quick Start и приступайте к прохождению туториала! 🚀
