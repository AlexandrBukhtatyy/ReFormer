# Видео 2 — сценарий съёмки

Сложная форма со всеми паттернами. Карта курса — в [parallel-tinkering-tarjan.md](parallel-tinkering-tarjan.md),
формат и легенда — как в [сценарии V1](reformer-course-v1-shooting-script.md).

**Домен:** `auto-loan` — заявка на автокредит, 3 шага.
**Хронометраж:** 82 минуты.
**Предпосылка:** зритель посмотрел V1 либо готов принять на веру, что модель, валидация и поведение —
разные слои. Заново это не объясняем.

**Код пишет агент, формат тот же, что в V1** — `[ПРОМПТ]` → `[ЭТАЛОН]` → `[ПРИЁМКА]`, промпты
агент-агностичны, MCP не подключён, контракт API лежит в `AGENTS.md`, ТЗ формы — в
`src/forms/auto-loan/spec.md` рядом с её кодом, как и в V1.
Правила игры здесь **не повторяются**: одна фраза «формат тот же, что в первом видео» в блоке 0.
Легенда маркеров и протокол работы с агентом — в
[сценарии V1](reformer-course-v1-shooting-script.md#как-читать); таблица промптов V2 — в приложении
ниже.

Выигрыш от генерации в V2 больше, чем в V1: здесь девятьсот строк вместо трёхсот, и почти всё —
однотипные пачки, которые в кадре смотреть незачем.

## Принцип отбора материала

Флагманская форма в монорепо — 3612 строк, потому что каждый паттерн повторён по 3–8 раз. Здесь
**каждый класс фич показывается ровно один раз**, повторы проговариваются словами. Ожидаемый объём —
900–1000 строк.

| Класс фич | В оригинале | В курсе | Блок |
| --- | --- | --- | --- |
| Шаги визарда | 6 | 3 | v2-01 |
| Динамические массивы | 3 | 1 | v2-06 |
| `compute` | 8 | 7 (5 из них — одной пачкой) | v2-03, v2-06 |
| `copyFrom` группы | ✓ | 1 | v2-05 |
| `enableWhen` + `resetOnDisable` | 5 блоков | 2 | v2-05 |
| Async-загрузка опций | 1 | 1 | v2-04 |
| Динамический `max` через `onChange` | 2 | 1 | v2-04 |
| Async-валидация | 1 | 1 | v2-07 |
| `cross` + `severity: 'warning'` | 5 | 1 | v2-07 |
| `each` per-row | ✓ | 1 | v2-06 |
| `AsyncBoundary` префилл | ✓ | 1 | v2-08 |
| FileUpload | ✓ | 1 | v2-08 |
| Свои операторы | 2 | 2 | v2-04, v2-06 |

**V2 работает только на одном таргете — core + ui-kit.** История про три рендерера рассказана в V1,
повтор убьёт темп.

---

## Блок 0 · Интро (0:00 – 3:00)

**[ЭКРАН]** `BROWSER` → готовая форма из тега `v2-09`, пройти три шага за 40 секунд.

**[РЕПЛИКА]** «Заявка на автокредит. Каскадный справочник, семь вычисляемых полей, копирование адреса,
динамический массив созаёмщиков, асинхронная проверка кода из СМС, предупреждение, которое не
блокирует отправку. Всё, что встречается в реальных формах — в одной».

**[ЭКРАН]** `EDITOR` → открыть флагман монорепо, показать `wc -l`: 3612 строк.

**[РЕПЛИКА]** «Вот эта же форма в полном объёме — три с половиной тысячи строк. Мы напишем тысячу.
Разница не в упрощении механик, а в том, что там каждая механика повторена пять раз, а здесь — один.
Где я срезаю повтор, я буду говорить об этом вслух».

**[ЯКОРЬ]** Кадр с `3612` рядом с готовой формой — задаёт масштаб честно.

**[РЕПЛИКА — одна фраза, правила не пересказываем]** «Формат тот же, что в первом видео: код пишет
агент по промптам из репозитория, я показываю промпт и разбираю результат. Кто не смотрел — правила
игры разобраны там в блоке два, здесь повторять не буду».

---

## Блок 1 · Каркас: модель раньше полей (3:00 – 10:00)

**Тег:** `v2-01-scaffold`

### 1.1 Тип (3:00 – 5:30)

**[РЕПЛИКА — до кода]** «Начинаем не с формы. Начинаем с типа данных, которые уедут на сервер. Это
не стилистика: если начать с полей, тип получится отпечатком верстки, а не предметной области».

**[ПРОМПТ]** `prompts/v2-01-scaffold.md`:

```
Заводим форму auto-loan — заявка на автокредит, три шага визарда.
Состав полей по шагам, числа и формулы — в src/forms/auto-loan/spec.md,
она уже лежит в этой директории.

types.ts: тип AutoLoanForm плюс Address и CoBorrower. Рядом с каждым полем,
которое будет вычисляемым, копируемым или условно доступным, оставь
комментарий-маркер: что с ним произойдёт дальше.
Эти комментарии — часть задачи, не убирай их.

model.ts: фабрика начального состояния и фабрика пустого созаёмщика.

create-form.ts: сборка на createCoreForm с моделью; схема, поведение и валидация
пока пустые — их наполним следующими шагами.

index.tsx: FormWizard из @reformer/ui-kit на три пустых шага, тело каждого шага —
отдельный React-компонент. Правил пока нет, config пустой.
```

**[РЕПЛИКА]** «Абзац про комментарии-маркеры — не украшение. Я прошу агента оставить в типе карту
того, что мы будем делать полтора часа. Дальше каждая стрелка превратится в строку кода, и это
удобный способ не потерять ни одной».

**[ЭТАЛОН]** `types.ts` — весь тип целиком, с комментариями-маркерами:

```ts
export interface Address { region: string; city: string; street: string; house: string; }

export interface CoBorrower {
  lastName: string; firstName: string;
  monthlyIncome: number | null;
  relation: 'spouse' | 'parent' | 'other';
}

export interface AutoLoanForm {
  // Шаг 1 · Автомобиль и кредит
  carBrand: string;
  carModel: string;              // ← async-опции по carBrand
  carYear: number | null;
  carPrice: number | null;
  initialPayment: number | null;
  loanTerm: number;              // ← динамический max от возраста
  loanAmount: number;            // ← compute
  interestRate: number;          // ← compute
  monthlyPayment: number;        // ← compute

  // Шаг 2 · Заявитель
  personal: { lastName: string; firstName: string; birthDate: string };
  fullName: string;              // ← compute
  age: number;                   // ← compute
  registrationAddress: Address;
  sameAsRegistration: boolean;
  residenceAddress: Address;     // ← copyFrom ГРУППЫ
  employmentStatus: 'employed' | 'selfEmployed' | 'unemployed';
  companyName: string;           // ← enableWhen + resetOnDisable
  companyInn: string;
  monthlyIncome: number | null;
  coBorrowers: CoBorrower[];     // ← единственный массив
  coBorrowersIncome: number;     // ← compute по массиву
  totalIncome: number;           // ← compute
  documents: File[];

  // Шаг 3 · Подтверждение
  paymentToIncomeRatio: number;  // ← compute → warning
  phone: string;
  smsCode: string;               // ← validateAsync
  agree: boolean;
}
```

**[РЕПЛИКА]** «Комментарии-стрелки остаются в файле специально. Это карта: к концу видео каждая
стрелка станет строкой в `form.behavior.ts` или в `validation.ts`».

**[ПРИЁМКА]** Обязано: маркеры на месте у всех вычисляемых, копируемых и условных полей; вложенные
группы (`personal`, адреса) описаны структурой, а не плоским списком с префиксами. Вправе отличаться:
формулировки комментариев, порядок полей внутри шага.

### 1.2 Модель и пустой визард (5:30 – 10:00)

**[ЭТАЛОН]** `model.ts` — фабрика начального состояния + фабрика пустого созаёмщика; `create-form.ts` —
`createCoreForm({ model, schema, behavior, validation })`, где пока всё, кроме модели, пустое;
`index.tsx` — `FormWizard` на три пустых шага. Визард берётся готовый из ui-kit, он принимает шаги
списком:

```tsx
import { FormWizard, type FormWizardStep } from '@reformer/ui-kit/form-wizard';
import type { FormWizardHandle } from '@reformer/cdk/form-wizard';

export const STEPS: FormWizardStep<AutoLoanForm>[] = [
  { number: 1, title: 'Автомобиль', icon: '🚗', body: CarStep },
  { number: 2, title: 'Заявитель',  icon: '👤', body: ApplicantStep },
  { number: 3, title: 'Подтверждение', icon: '✓', body: ConfirmStep },
];

const navRef = useRef<FormWizardHandle<AutoLoanForm>>(null);
const { form, validation } = useFormBundle(createAutoLoanForm);

<FormWizard ref={navRef} form={form} config={validation} steps={STEPS} onSubmit={submit} />
```

**[РЕПЛИКА]** «`config` — это собранная валидация из `defineSteps`. Визард не знает правил, он знает,
у какого шага их спросить: перед переходом дальше вызывает валидацию текущего шага и не пускает, пока
она не пройдёт».

**[РЕПЛИКА]** «Тело шага — обычный React-компонент, ему приезжает `control`. То есть каждый шаг можно
разрабатывать и ревьюить отдельно, а в headless-варианте из `@reformer/cdk` разметку визарда можно
написать целиком свою».

**[ДЕЙСТВИЕ]** Показать в браузере: три пустых шага, навигация работает.

**[РЕПЛИКА]** «Форма пустая, но уже живая. Дальше мы наполняем её слоями, и каждый слой — отдельный
файл, который можно ревьюить отдельно».

**[ПРИЁМКА]** Обязано: сборка через `useFormBundle`; тело шага — отдельный компонент, а не инлайн;
навигация работает при пустом `config`. Вправе отличаться: иконки, заголовки шагов, вёрстка.

**[РИСК]** Не увязнуть в вёрстке шагов. Разметка шагов — из тега, в кадре только структура.

---

## Блок 2 · Шаг 1: поля и правила шага (10:00 – 17:30)

**Тег:** `v2-02-step1-fields`

### 2.1 Схема шага (10:00 – 13:30)

**[ПРОМПТ]** `prompts/v2-02-step1.md`:

```
Наполни первый шаг «Автомобиль и кредит».

form.schema.ts: марка, модель, год, цена, первый взнос, срок кредита плюс три
вычисляемых поля — сумма кредита, ставка, ежемесячный платёж. Вычисляемые
описывай обычными полями с readOnly: true, никакого особого типа узла.

validation.ts: правила первого шага через defineSteps. Ключ шага — селектор,
не индекс. Шаг без правил помечай явным null, а не пропуском ключа.
Cross-field правила клади в extras, они проверяются только при полной отправке.
Стратегия — afterFirstSubmit с задержкой 400 мс.

Формулы не пиши, поведения не пиши. Только поля и правила.
```

**[ЭТАЛОН]** блок «Автомобиль и кредит» в `form.schema.ts` — марка, модель, год, цена, первый взнос,
срок; три вычисляемых поля с `readOnly: true`.

**[РЕПЛИКА]** «Вычисляемые поля в схеме — обычные поля с `readOnly`. Не «выведенное значение», не
особый тип узла. Для схемы они неотличимы от вводимых, и это правильно: она не должна знать, откуда
взялось число».

### 2.2 Валидация по шагам (13:30 – 17:30)

**[ЭТАЛОН]** `validation.ts` с `defineSteps`:

```ts
export const autoLoanValidation = defineSteps(model, {
  steps: {
    car:     stepCar,
    applicant: stepApplicant,
    confirm: stepConfirm,
  },
  extras: crossRules,
  strategy: 'afterFirstSubmit',
  debounce: 400,
});
```

**[РЕПЛИКА — ключевая для архитектора]** «Ключ — селектор шага, не индекс. Вставите шаг в середину —
ничего не разъедется. И `null` для шага без правил пишется явно: опечатка в имени ключа иначе
выглядела бы как «у шага просто нет правил», а это самая тихая ошибка из возможных».

**[РЕПЛИКА]** про `extras`: «Cross-field правила живут отдельно от шагов. Они проверяются только при
полной отправке — потому что на втором шаге ещё нечего сравнивать с пятым».

**[РЕПЛИКА]** про стратегию: «`afterFirstSubmit` — молчим, пока пользователь не нажал «Далее» первый
раз, потом подсвечиваем живьём с задержкой 400 мс. Валидация, которая кричит на пустое поле, в
которое ещё не начали печатать, — это не помощь».

**[ПРИЁМКА]** Обязано: ключи шагов — селекторы, а не индексы; шаг без правил помечен явным `null`;
cross-field правила в `extras`, а не внутри шага. Вправе отличаться: тексты сообщений, состав
базовых правил. Проверка — следующее действие в браузере.

**[ДЕЙСТВИЕ]** В браузере: нажать «Далее» с пустыми полями → ошибки; начать печатать → гаснут.

---

## Блок 3 · Вычисляемые поля (17:30 – 27:00)

**Тег:** `v2-03-computed`

### 3.1 Проверка графа до кода (17:30 – 19:30)

**[ЭКРАН]** `TERM` / панель MCP

**[ДЕЙСТВИЕ — руками, до всякой генерации]** Прогнать через `check_behaviors`:

```
loanAmount     ← carPrice, initialPayment
interestRate   ← carPrice, loanTerm, carYear
monthlyPayment ← loanAmount, interestRate, loanTerm
```

**[РЕПЛИКА]** «Сначала граф, потом код. Циклическая зависимость в реактивной системе — ошибка, которую
больно ловить в рантайме: она проявляется как «форма зависла» или «значение скачет». Здесь она
ловится статически, за секунду, до первой строки».

**[РЕПЛИКА — та же оговорка, что в V1]** «И это единственное место, где я беру инструмент из
MCP-сервера. Промпты работают в любом агенте, а вот эта проверка — отдельная история, ей посвящён
отдельный ролик».

### 3.2 Behaviors (19:30 – 24:00)

**[ПРОМПТ]** `prompts/v2-03-computed.md`:

```
Напиши form.behavior.ts на defineFormBehavior. Формулы — в spec.md.

Первым шагом только три вычисляемых поля первого шага: сумма кредита, ставка,
ежемесячный платёж по аннуитету. Списки зависимостей не перечисляй.

Затем добавь пачкой остальные вычисляемые поля второго и третьего шага —
полное имя, возраст, суммарный доход с учётом созаёмщиков, отношение платежа
к доходу. Пачку оформи одним блоком без комментариев: механика у всех одинаковая.

Плюс __tests__/behavior.test.ts на модели, без рендера: аннуитет проверь
на конкретных числах из spec.md.
```

**[РЕПЛИКА]** «Смотрите, как устроен промпт. Я явно прошу сначала три поля, потом остальные пачкой —
потому что три надо разобрать голосом, а пачку смотреть незачем. Промпт управляет не только кодом,
но и монтажом».

**[ЭТАЛОН]** `form.behavior.ts`:

```ts
export const autoLoanBehavior = defineFormBehavior<AutoLoanForm>(({ model, form }) => {
  compute(model.$.loanAmount,     () => (model.carPrice ?? 0) - (model.initialPayment ?? 0));
  compute(model.$.interestRate,   () => rateFor(model.carYear, model.loanTerm));
  compute(model.$.monthlyPayment, () => annuity(model.loanAmount, model.interestRate, model.loanTerm));
});
```

**[РЕПЛИКА]** «Три строки. Ни одного списка зависимостей — они выводятся из того, что функция
прочитала».

**[РЕПЛИКА — важная оговорка]** «В настоящей заявке таких `compute` восемь. Механика ровно та же, и
поэтому остальные пять я напишу одной пачкой без комментариев — смотреть на них по отдельности
незачем».

**[ДЕЙСТВИЕ]** Пролистать пачку — `fullName`, `age`, `totalIncome`, `paymentToIncomeRatio` — не
задерживаясь.

**[ПРИЁМКА]** Обязано: ни одного массива зависимостей; аннуитет считается по формуле из `spec.md`,
а не приближением; поля второго и третьего шага не разъехались с типом. Вправе отличаться: имена
хелперов `rateFor`/`annuity`, порядок операторов.

**[РИСК]** Аннуитет — единственная формула курса, которую агент действительно может переписать
«похоже, но неверно». Тест из этого же промпта её и ловит; если он красный, это не косметика.

### 3.3 Тесты поведения (24:00 – 27:00)

**[ЭТАЛОН]** `__tests__/behavior.test.ts` — вычисления проверяются на модели, без рендера:

```ts
const { model } = createAutoLoanForm();
model.carPrice = 2_000_000;
model.initialPayment = 500_000;
expect(model.loanAmount).toBe(1_500_000);
```

**[РЕПЛИКА]** «Формула аннуитета — это то, из-за чего в проде бывают судебные иски. Она обязана быть
покрыта тестом, и она покрывается тестом без единого клика».

---

## Блок 4 · Свои операторы и асинхронные справочники (27:00 – 36:30)

**Тег:** `v2-04-async-options`

### 4.1 Проблема (27:00 – 29:00)

**[РЕПЛИКА]** «Марка → модель. Классический каскад: выбрали марку, подгрузили модели, старую модель
сбросили. Встроенного оператора для этого нет — и не должно быть, потому что вариантов поведения тут
десяток. Зато контракт открыт».

### 4.2 Свой оператор (29:00 – 33:00)

**[ПРОМПТ]** `prompts/v2-04-operators.md`:

```
Каскадный справочник: выбрали марку — подгрузились модели, прежняя модель сбросилась.
Встроенного оператора для этого в библиотеке нет.

Напиши свой оператор loadOptionsOn в operators.ts. Он должен собираться
из уже существующих примитивов — onChange плюс обновление componentProps
целевого поля, — а не лезть во внутренности библиотеки. Параметры: debounce
и нужно ли сбрасывать целевое поле.

Гонку ответов обработай: пользователь переключает марку быстрее, чем приходят
ответы, и запросы возвращаются в произвольном порядке. У onChange есть
AbortSignal — проверь его перед записью результата.

Отдельно, тем же файлом поведения: максимум срока кредита зависит от возраста
заявителя. Это ограничение ввода, а не валидация. Формула — в spec.md.
```

**[РЕПЛИКА]** «Абзац про гонку я пишу явно, потому что это ровно то место, где генерация обычно даёт
правдоподобный и неверный код. Промпт с описанием проблемы работает лучше промпта с описанием
решения: если сказать «добавь проверку `signal.aborted`», агент добавит её и там, где она не нужна».

**[ЭТАЛОН]** `operators.ts`:

```ts
export function loadOptionsOn<TValue, TOption>(
  source: ReadonlySignal<TValue>,
  target: OptionsTarget,
  fetcher: (value: TValue) => Promise<{ data: TOption[] }>,
  options: { debounce?: number; resetTarget?: boolean } = {}
): void {
  const { debounce = 300, resetTarget = false } = options;
  onChange(source, async (value, { signal }) => {
    if (resetTarget) target.reset();
    const { data } = await fetcher(value);
    if (signal.aborted) return;              // ответ устарел — выбрасываем
    target.updateComponentProps({ options: data });
  }, { debounce });
}
```

**[РЕПЛИКА — главная в блоке]** «Смотрите, из чего он сделан: один `onChange` и `updateComponentProps`.
Это не плагин и не расширение — это обычная функция, которая вызывается внутри `defineFormBehavior`.
На месте использования её не отличить от встроенной».

**[РЕПЛИКА про `signal.aborted`]** «Вот это — не украшение. Пользователь быстро переключил марку три
раза: три запроса ушли, вернуться могут в любом порядке. Без проверки в поле окажутся модели от
второй марки, а выбрана третья. `onChange` отменяет предыдущий вызов сам, наше дело — проверить флаг
перед записью».

### 4.3 Динамический предел (33:00 – 36:30)

**[ЭТАЛОН]**

```ts
loadOptionsOn(model.$.carBrand, form.carModel, fetchCarModels, { resetTarget: true });

onChange(model.$.age, (age) =>
  form.loanTerm.updateComponentProps({ max: Math.min(Math.max(70 - age, 1) * 12, 84) })
);
```

**[РЕПЛИКА]** «Кредит должен быть погашен до семидесяти лет. Это не валидация — это ограничение
ввода: правильнее не дать выбрать невозможный срок, чем дать и потом отругать».

**[ПРИЁМКА]** Обязано: оператор собран из `onChange` и обновления пропсов, без обращения к
внутренностям библиотеки; `signal.aborted` проверяется **после** await и **до** записи; максимум срока
меняется через `updateComponentProps`, а не валидатором. Вправе отличаться: имя оператора, значение
debounce по умолчанию, сигнатура параметров.

**[ДЕЙСТВИЕ]** В браузере: сменить марку → модели подгрузились, поле сбросилось; изменить дату
рождения → максимум срока поехал.

---

## Блок 5 · Группы: копирование и условная доступность (36:30 – 45:30)

**Тег:** `v2-05-groups`

### 5.1 Вложенные группы (36:30 – 39:00)

**[ПРОМПТ]** `prompts/v2-05-groups.md`:

```
Второй шаг, работа с группами.

Схема: блоки personal, registrationAddress, residenceAddress — вложенными
группами, как они лежат в типе.

Поведение, два разных случая, и их важно не перепутать:
1) адрес проживания копируется из адреса регистрации, пока стоит галочка
   «совпадает» — копируется группа целиком, не поле за полем; на время
   копирования группа выключена, но значение НЕ сбрасывается: оно осмысленное.
2) название и ИНН работодателя доступны только при подходящем статусе занятости
   и при выключении сбрасываются: сохранить работодателя у безработного нельзя.

Разница между этими случаями — в том, сбрасывается значение или нет. Проставь
её явно и нигде не полагайся на умолчание.
```

**[ЭТАЛОН]** блоки `personal`, `registrationAddress`, `residenceAddress` в схеме.

**[РЕПЛИКА]** «Группа в модели — обычный вложенный объект. Никакой регистрации, никакого
`useFieldArray`».

### 5.2 Копирование группы целиком (39:00 – 41:30)

**[ЭТАЛОН]**

```ts
copyFrom(model.registrationAddress, model.residenceAddress, {
  when: () => model.sameAsRegistration,
});
enableWhen(model.residenceAddress, () => !model.sameAsRegistration);
```

**[РЕПЛИКА]** «`copyFrom` принимает не поле, а группу — копируются все четыре поля адреса разом. И
обратите внимание на связку: копируем **и** выключаем. Выключаем без `resetOnDisable`, потому что
значение там осмысленное — оно скопировано».

### 5.3 Разница, которую все путают (41:30 – 45:30)

**[ЭТАЛОН]**

```ts
enableWhen([model.$.companyName, model.$.companyInn],
  () => model.employmentStatus === 'employed',
  { resetOnDisable: true }
);
```

**[РЕПЛИКА — ключевая в блоке]** «Два `enableWhen` подряд, и они ведут себя по-разному. Адрес
проживания выключается **без** сброса — значение скопировано и должно уехать на сервер. Работодатель
выключается **со** сбросом — если человек перешёл в статус «безработный», старое название компании в
заявке недопустимо. По умолчанию `resetOnDisable` — `false`, то есть библиотека отказывается угадывать
за вас. Это тот случай, когда умолчание должно быть неудобным».

**[ДЕЙСТВИЕ]** В браузере показать оба: галочка «адрес совпадает» → поля заполнились и погасли;
статус «безработный» → поля компании погасли и очистились.

**[РЕПЛИКА]** «Таких блоков `enableWhen` в настоящей заявке пять — по одному на каждый тип занятости
и тип кредита. Механика та же, повторять не буду».

**[ПРИЁМКА — единственное, что здесь важно]** `resetOnDisable` проставлен **явно** в обоих
`enableWhen` и значения у них разные. Если агент положился на умолчание хоть в одном — правим
репликой: весь смысл подблока в том, что умолчание тут не выбирается за вас. Вправе отличаться:
объединены ли `companyName` и `companyInn` в один вызов или разнесены.

**[ЯКОРЬ]** Кадр, где на экране одновременно видны оба поведения — с сбросом и без.

---

## Блок 6 · Массив созаёмщиков (45:30 – 56:30)

**Тег:** `v2-06-array`

### 6.1 Массив в схеме (45:30 – 49:00)

**[ПРОМПТ]** `prompts/v2-06-array.md`:

```
Динамический массив созаёмщиков.

Схема: узел массива с item и initialValue. initialValue должен быть фабрикой,
а не объектом-литералом. Разметка строки — FormArraySection в теле шага.

Поведение: суммарный доход созаёмщиков и общий доход. Сумму по массиву считай
обычным compute. В библиотеке есть оператор aggregateInto — он НЕ для этого,
он патчит строки массива; не бери его.

Плюс: при снятии галочки «есть созаёмщики» массив очищается.

Валидация: правила на строку через each — фамилия и доход. Объявляются один раз,
применяются ко всем строкам, ошибка приезжает в свою строку.
```

**[РЕПЛИКА]** «Средний абзац — это отвод от ловушки. `aggregateInto` по названию выглядит ровно как
то, что нужно для суммы по массиву, и агент туда идёт охотно. На самом деле он патчит строки. Такие
предупреждения — второй тип содержимого контекст-файла: не «как правильно», а «куда не сворачивать»».

**[ЭТАЛОН]** узел массива в `form.schema.ts` + `FormArraySection` в разметке шага.

**[РЕПЛИКА]** «`initialValue` — фабрика, а не объект. Общий объект-литерал дал бы всем строкам одну
ссылку, и правка одной правила бы все».

### 6.2 Поведение и агрегация (49:00 – 52:30)

**[ЭТАЛОН]**

```ts
compute(model.$.coBorrowersIncome,
  () => model.coBorrowers.map(c => c.monthlyIncome ?? 0).reduce((a, b) => a + b, 0));

compute(model.$.totalIncome, () => (model.monthlyIncome ?? 0) + model.coBorrowersIncome);

clearWhenOff(model.$.hasCoBorrowers, form.coBorrowers);
```

**[РЕПЛИКА — предупреждение]** «Сумма по массиву — это обычный `compute`. В библиотеке есть оператор
`aggregateInto`, и по названию кажется, что он для этого. Нет: он **патчит строки массива**, его
колбэк возвращает список `{ index, patch }`. Для свёртки массива в одно поле нужен `compute`».

### 6.3 Правила на строку (52:30 – 56:30)

**[ЭТАЛОН]**

```ts
each(model.coBorrowers, (c) => {
  validate(c.$.lastName,      [required(), minLength(2)]);
  validate(c.$.monthlyIncome, [required(), min(1)]);
});
```

**[РЕПЛИКА]** «Правила объявлены один раз, применяются к каждой строке, ошибки приезжают в свою
строку. Добавили созаёмщика — правила уже там».

**[ДЕЙСТВИЕ]** В браузере: добавить две строки, оставить одну пустой → ошибка ровно в ней; заполнить →
общий доход пересчитался; снять галочку → массив очистился.

**[ПРИЁМКА]** Обязано: `initialValue` — фабрика; сумма считается `compute`, а не `aggregateInto`;
правила строки объявлены один раз через `each`. Вправе отличаться: имя флага «есть созаёмщики»,
состав правил строки.

**[РЕПЛИКА]** «В настоящей заявке таких массивов три — имущество, действующие кредиты, созаёмщики.
Все три устроены одинаково».

---

## Блок 7 · Асинхронная валидация и предупреждения (56:30 – 65:30)

**Тег:** `v2-07-async-validation`

### 7.1 Код из СМС (56:30 – 61:00)

**[ПРОМПТ]** `prompts/v2-07-async.md`:

```
Асинхронная проверка кода из СМС и предупреждение, которое не блокирует отправку.

Асинхронное правило на поле кода. Три вещи, без которых оно разваливается в проде:
не дёргать сервер, пока код введён не полностью; выбрасывать устаревший ответ,
если пользователь стёр и ввёл заново (у правила есть AbortSignal); задержку задавать
стратегией на уровне схемы, а не таймером внутри правила.

Отдельно, в extras: если платёж превышает половину дохода — показать предупреждение,
которое подсвечивает поле, но НЕ блокирует отправку формы. У ошибок валидации для
этого есть уровень severity; отдельный стейт предупреждений заводить не надо.

Почему в extras, а не в шаге: отношение платежа к доходу нельзя посчитать,
пока не пройдены оба предыдущих шага.
```

**[ЭТАЛОН]**

```ts
const smsCodeValid: AsyncRule<string> = async (code, _model, { signal }) => {
  if (!code || code.length < 4) return null;          // нечего проверять
  const ok = await verifySmsCode(code, { signal });
  return ok ? null : { code: 'sms', message: 'Неверный код' };
};

validateAsync(model.$.smsCode, [smsCodeValid]);
```

**[РЕПЛИКА]** «Три вещи, без которых асинхронный валидатор в проде разваливается. Первая — ранний
выход: не дёргаем сервер, пока введено два символа из четырёх. Вторая — `signal`: пользователь стёр
и ввёл заново, старый ответ обязан быть выброшен, иначе он перезапишет свежий. Третья — задержка,
она задана стратегией на уровне схемы, а не внутри правила».

**[ДЕЙСТВИЕ]** В браузере: ввести неверный код → ошибка; быстро исправить → показать в Network, что
устаревший ответ не влияет на поле.

### 7.2 Предупреждение, которое не блокирует (61:00 – 65:30)

**[ЭТАЛОН]** в `extras`:

```ts
cross(model.$.paymentToIncomeRatio, (f) =>
  f.paymentToIncomeRatio > 0.5
    ? { code: 'highLoad', severity: 'warning',
        message: 'Платёж превышает половину дохода — заявку могут отклонить' }
    : null
);
```

**[РЕПЛИКА — сильный аргумент]** «`severity: 'warning'`. Поле подсвечено, текст показан, но
`validateAll` вернёт `true` и форма отправится. Это ровно то, чего обычно добиваются костылями:
отдельным стейтом «предупреждения», который живёт мимо валидации и рассинхронизируется с ней.
Здесь предупреждение — та же ошибка, просто не блокирующая».

**[ДЕЙСТВИЕ]** Показать: жёлтая подсветка, форма отправляется.

**[РЕПЛИКА]** «Такие правила лежат в `extras`, а не в шаге: соотношение платежа к доходу нельзя
посчитать, пока не пройдены оба шага».

**[ПРИЁМКА]** Обязано: ранний выход до сетевого вызова; `signal` проверяется после await;
предупреждение сделано уровнем ошибки, а не отдельным стейтом, и `validateAll` при нём возвращает
`true`. Вправе отличаться: длина кода, порог отношения платежа к доходу (если он не задан в
`spec.md`), тексты.

---

## Блок 8 · Файлы и предзаполнение (65:30 – 75:00)

**Тег:** `v2-08-files-prefill`

### 8.1 Загрузка документов (65:30 – 69:00)

**[ПРОМПТ]** `prompts/v2-08-files-prefill.md`:

```
Документы и режим редактирования.

Поле документов на FileUploadField, файловые валидаторы — количество, размер,
типы. Файлы в модели — обычное поле File[], не отдельная подсистема.

Режим редактирования: форма открывается по ?id=… и должна уметь существовать
в трёх состояниях — грузится, загрузилась, упала. Возьми AsyncBoundary
из ui-kit в self-managed режиме: снаружи задаются только «как загрузить»
и «что сделать с ответом». Состояния, повтор и отмену устаревшего запроса
при смене ключа не переписывай — они внутри компонента.

Отмена важна: пользователь открыл одну заявку, передумал, открыл другую.

Submit: прогнать полную валидацию и отправить model.get() как есть,
без сериализации контролов.
```

**[ЭТАЛОН]** поле `documents` с `FileUploadField` + валидаторы файлов:

```ts
validate(model.$.documents, [maxFiles(5), maxFileSize(10 * 1024 * 1024), fileType(['application/pdf', 'image/*'])]);
```

**[РЕПЛИКА]** «Файловые валидаторы — такие же правила, как `required`. Файл в модели — обычное поле
типа `File[]`».

### 8.2 Режим редактирования (69:00 – 75:00)

**[РЕПЛИКА — постановка]** «Заявку можно открыть на редактирование. Значит форма должна уметь
существовать в трёх состояниях: грузится, загрузилась, упала. И это состояние — не про форму, а про
экран вокруг неё».

**[ЭТАЛОН]** `AsyncBoundary` из ui-kit — self-managed режим:

```tsx
<AsyncBoundary<AutoLoanBundle>
  load={(signal) => loadApplication(applicationId!, signal)}
  loadKey={applicationId}
  enabled={applicationId !== null}
  onSuccess={(bundle) => applyApplication(form, bundle)}
>
  <FormWizard ref={navRef} form={form} config={validation} steps={STEPS} onSubmit={submit} />
</AsyncBoundary>
```

**[РЕПЛИКА]** «Снаружи остаются только два вопроса: как загрузить и что сделать с ответом. Состояния,
отмена устаревшего запроса при смене `loadKey`, кнопка «Повторить» и ARIA-атрибуты — внутри
компонента».

**[РЕПЛИКА]** про `signal`: «Пользователь открыл заявку, передумал, открыл другую. Первый запрос
отменяется — иначе он вернётся позже и затрёт вторую форму данными первой».

**[РЕПЛИКА — вилка]** «Это готовый компонент из ui-kit. Если нужна своя разметка состояний, в
`@reformer/cdk` лежит headless-версия того же самого — `AsyncBoundary.Loading`, `.Content`, `.Error`,
`.Retry` — и вы пишете вёрстку сами».

**[ДЕЙСТВИЕ]** Открыть `?id=1` → скелетон → заполненная форма. Сломать эндпоинт → панель ошибки,
нажать «Повторить».

**[ЭТАЛОН]** Submit:

```tsx
if (await validation.validateAll()) await saveApplication(model.get());
```

**[РЕПЛИКА]** «`model.get()` — чистый объект нужного типа. Не `formState.values`, не сериализация
контролов. Тип, который мы написали в первом блоке, — то же самое, что уходит на сервер».

**[ПРИЁМКА]** Обязано: `AsyncBoundary` взят готовым, состояния загрузки не переписаны руками;
`loadKey` проброшен, иначе отмена не работает; submit отправляет `model.get()` без промежуточного
преобразования. Вправе отличаться: лимиты файловых валидаторов, вёрстка панели ошибки.

---

## Блок 9 · Итог (75:00 – 82:00)

**Тег:** `v2-09-wrapup`

### 9.1 Тесты правил (75:00 – 78:00)

**[ДЕЙСТВИЕ]** `npm test` — полный `validation.test.ts`: невалидная заявка, валидная, граничные случаи
по возрасту и сроку, предупреждение не блокирует.

**[РЕПЛИКА]** «Вся эта сложность проверена без единого клика».

### 9.2 Карта файлов (78:00 – 80:00)

**[ЭКРАН]** `EDITOR` → дерево модуля.

| Файл | Отвечает за | Строк |
| --- | --- | --- |
| `types.ts` | контракт данных | ~60 |
| `model.ts` | начальное состояние | ~50 |
| `form.behavior.ts` | вычисления и реакции | ~90 |
| `validation.ts` | правила | ~220 |
| `operators.ts` | свои операторы | ~40 |
| `form.schema.ts` | вёрстка полей | ~400 |
| `data-sources.ts` / `api.ts` | справочники и сеть | ~80 |

**[РЕПЛИКА]** «Самый большой файл — схема, то есть вёрстка. Логика — это `behavior` плюс `validation`,
триста строк на форму такой сложности. И ревьюить их можно отдельно от вёрстки, потому что они в
отдельных файлах не по соглашению, а по устройству библиотеки».

### 9.3 Чеклист и анонс (80:00 – 82:00)

Проговорить как чеклист для своих форм:

1. Тип данных — раньше полей.
2. Граф вычислений — через `check_behaviors`, до кода.
3. Правила — по селекторам шагов, cross-field в `extras`.
4. `resetOnDisable` — осознанное решение на каждом `enableWhen`.
5. Асинхронный валидатор — ранний выход, `signal`, задержка в стратегии.
6. Некритичное — `severity: 'warning'`, а не отдельный стейт.
7. Повторяющаяся механика — свой оператор в `operators.ts`.

**[РЕПЛИКА]** «Этот `form.behavior.ts` и этот `validation.ts` уедут в renderer-react или в JSON без
единой правки — ровно как в первом видео, теги `v1-08` и `v1-09»`.

**[РЕПЛИКА — про промпты, одна фраза]** «И ещё одно наблюдение по итогам восьми промптов. Самое
полезное, что в них было, — не описание того, как надо, а предупреждения о том, куда не сворачивать:
`aggregateInto` не для сумм, `resetOnDisable` не угадывается, устаревший ответ надо выбрасывать. Это
и есть содержимое контекст-файла проекта, и это же — то, что MCP-сервер отдаёт агенту сам. Про него
отдельный ролик».

Анонсы спутников: UI-Builder, формы в микрофронтах, MCP-сервер.

---

## Приложение · Сверенные контракты V2

```ts
// @reformer/cdk
defineSteps<Sel, T>(model, config: {
  steps: Record<Sel, ValidationSchema<T> | null>;   // null — «шаг без правил» ЯВНО
  extras?: ValidationSchema<T>;
  strategy?: ValidationStrategyKind;                // по умолчанию 'submit'
  debounce?: number;
  liveAfterSubmit?: 'change' | 'blur';
}): WizardStepsConfig<Sel>

// headless-слой (@reformer/cdk/form-wizard)
FormWizardProps<T>  = { form, config, children?, onStepChange?, scrollToTop? }
FormWizardHandle<T> = { form, currentStep, completedSteps,
                        validateCurrentStep(), goToNextStep(), submit(), … }

// готовый визард (@reformer/ui-kit/form-wizard) — его и берём в V2
<FormWizard ref form={form} config={validation} steps={STEPS} onSubmit={…} />
FormWizardStep<T>   = { number, title, icon?, body: ComponentType<{ control }> }

// AsyncBoundary существует в ДВУХ видах:
//   @reformer/ui-kit — self-managed: { load(signal), loadKey, enabled, onSuccess }
//   @reformer/cdk    — headless compound: .Root .Idle .Loading .Content .Error .Retry .Empty

// Кастомные операторы — НЕ из библиотеки, пишутся в operators.ts
loadOptionsOn(source, target, fetcher, { debounce = 300, resetTarget = false })
clearWhenOff(flag, array)     // → array.clear()

// severity
// ValidationError.severity === 'warning' не влияет на валидность:
// проверяется как `e.severity !== 'warning'` в field-node и в validateModel
```

### Ловушки блока

1. **`aggregateInto` — не свёртка.** Патчит строки массива (`{ index, patch }[]`). Сумма по массиву —
   обычный `compute`. Проговаривается вслух в блоке 6.
2. **`apply` в behaviors и в validation — разные функции.** `apply(targets, subSchema)` против
   вариадического `apply(...schemas)`; `defineSteps` использует второй.
3. **Стратегия по умолчанию `'submit'`.** Для живой подсветки в кадре нужен `afterFirstSubmit`.
4. **`initialValue` массива — фабрика.** Литерал даст всем строкам общую ссылку.
5. **Ключи `steps` — селекторы, не индексы.** `null` пишется явно.

---

## Приложение · Промпты V2

Восемь промптов, по одному на тег; финальный блок `v2-09-wrapup` ничего не порождает — там только
прогон тестов и чеклист. Формат, протокол отклонения и правила проходимости — те же, что в
[V1, приложение D](reformer-course-v1-shooting-script.md#приложение-d--промпты); контракт API лежит в
общем `AGENTS.md`, числа и формулы автокредита — в `spec.md`.

| Тег | Промпт | Порождает | Чем проверяется |
| --- | --- | --- | --- |
| `v2-01-scaffold` | `prompts/v2-01-scaffold.md` | `types.ts`, `model.ts`, `create-form.ts`, `index.tsx` | маркеры в типе, навигация визарда живая |
| `v2-02-step1-fields` | `prompts/v2-02-step1.md` | шаг 1 в схеме + `defineSteps` | ключи-селекторы, явный `null` |
| `v2-03-computed` | `prompts/v2-03-computed.md` | `form.behavior.ts` + `behavior.test.ts` | зелёный тест аннуитета |
| `v2-04-async-options` | `prompts/v2-04-operators.md` | `operators.ts` + динамический `max` | `signal.aborted` после await |
| `v2-05-groups` | `prompts/v2-05-groups.md` | группы, `copyFrom`, два `enableWhen` | `resetOnDisable` явный в обоих |
| `v2-06-array` | `prompts/v2-06-array.md` | массив, агрегация, `each` | сумма через `compute`, не `aggregateInto` |
| `v2-07-async-validation` | `prompts/v2-07-async.md` | `validateAsync` + `severity: 'warning'` | `validateAll` при варнинге → `true` |
| `v2-08-files-prefill` | `prompts/v2-08-files-prefill.md` | файлы, `AsyncBoundary`, submit | `loadKey` проброшен, отмена работает |
| — | без промпта | прогон `check_behaviors` (§3.1), браузерные демо, `npm test` | делается руками |

### `src/forms/auto-loan/spec.md`

Устроена по образцу [V1, приложение E](reformer-course-v1-shooting-script.md#приложение-e--specmd-целиком)
и лежит там же — в директории формы, на теге `v2-01` единственным файлом. Полностью не расписываю:
структура та же, меняется домен. Обязана зафиксировать то, что уже проверяется тестами и репликами
сценария:

| Что | Где всплывает |
| --- | --- |
| Состав полей по трём шагам, включая вложенные группы и строку созаёмщика | §1.1, промпт `v2-01` |
| Сумма кредита = цена минус первый взнос | §3.3, `expect(model.loanAmount).toBe(1_500_000)` при цене 2 000 000 и взносе 500 000 |
| Ставка по году выпуска и сроку — таблицей, не формулой | §3.2, `rateFor` |
| **Формула аннуитета целиком**, с округлением | §3.2 и §3.3 — единственная формула курса, из-за которой в проде бывают иски |
| Возраст — полных лет на дату подачи, не на сегодня | §3.2, иначе тест зависит от дня прогона |
| Кредит гасится до 70 лет, потолок срока 84 месяца | §4.3, динамический `max` |
| Порог отношения платежа к доходу — 0.5 | §7.2, предупреждение со `severity: 'warning'` |
| Каскад марка → модель: список марок и по паре моделей на каждую | §4.2, `loadOptionsOn` |
| Лимиты файлов: количество, размер, типы | §8.1 |
| Даты в тестах валидации — относительные, не литералы | то же правило, что в V1 |

**Что заметно по этой таблице.** Самая ценная часть каждого промпта — не описание задачи, а
предупреждение о ловушке: `aggregateInto` не для сумм, `resetOnDisable` не угадывается, устаревший
ответ надо выбрасывать. Пять из восьми промптов содержат такой абзац, и все пять ловушек уже описаны
в разделе «Ловушки блока» выше. Это тот же список, только повёрнутый к агенту, — и то же самое,
что MCP-сервер отдаёт сам. Реплика об этом — в §9.3.

---

## Статус

Готов к записи после сборки репо-компаньона до тега `v2-09` **и прогона восьми промптов с
зафиксированной проходимостью** (`prompts/PASSRATE.md`). Порядок работ — как в V1: собрать репо по
промптам → снять → переставить теги по факту записи.

Сценарии спутников — в [reformer-course-satellites-shooting-scripts.md](reformer-course-satellites-shooting-scripts.md).
